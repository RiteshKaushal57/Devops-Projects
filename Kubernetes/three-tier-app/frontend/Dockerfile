# Use official Nginx image
FROM nginx:alpine

# Copy your HTML (and any JS/CSS files) to Nginx default directory
COPY . /usr/share/nginx/html

# Expose port 80 for HTTP traffic
EXPOSE 80

# Nginx automatically starts
